{
  "extends": "stylelint-config-standard",
  "syntax": "postcss-syntax",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": [
          "tailwind",
          "layer",
          "apply",
          "config"
        ]
      }
    ]
  }
}
